# Completed

Nothing has shipped yet.

When a milestone or feature is verified and merged, move its document here from `in-development/`, set `Status: shipped`, add a `Shipped:` date, and edit the body so it describes what was actually built rather than what was planned.
