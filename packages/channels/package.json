{
  "name": "@helpdock/channels",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Channel adapter interface and per-channel implementations.",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "test": "vitest run --config ../../vitest.config.ts --project @helpdock/channels"
  },
  "dependencies": {
    "@helpdock/schemas": "workspace:*",
    "nodemailer": "10.0.10",
    "sanitize-html": "2.17.7"
  },
  "devDependencies": {
    "@types/sanitize-html": "2.16.1"
  }
}
